import { Vector, Quotient } from "../../numeric"

const HALF_SCALER = [1, 2] as Quotient

const IRRATIONAL_SCALED_VECTOR_BASE_VECTOR = [1] as Vector<{ rational: true }>

export { IRRATIONAL_SCALED_VECTOR_BASE_VECTOR, HALF_SCALER }
